# Page Not Found

This page may have moved during the resource cleanup. Try the [Resource Center](resources/index.md) if the redirect does not continue automatically.

<script>
(function () {
  var base = "/My_Notes_Four_ZJE";
  var legacyDetailAliases = {
    "ADS2_exam_skill_bundle": "ADS2_Yiru_exam_skill_bundle",
    "BG2_sum_Yiru": "BG2_Yiru_summary",
    "Code_Cheatsheet_Yiru": "Code_Cheatsheet_Yiru_collection",
    "GP2_sum_Yiru": "GP2_Yiru_summary",
    "IBMS3_full_Yiru": "IBMS3_Yiru_full_notes",
    "IFBS（theme34)": "IFBS2_Yue_theme3_theme4",
    "IID_4_full_Yiru": "IID4_Yiru_full_notes",
    "MBE3_sum_Yiru": "MBE3_Yiru_summary",
    "PoN3_full_Yiru": "PoN3_Yiru_full_notes",
    "pon": "PoN3_Yue_notes"
  };

  function decodedPathname() {
    try {
      return decodeURIComponent(window.location.pathname);
    } catch (error) {
      return window.location.pathname;
    }
  }

  function stripBase(pathname) {
    if (pathname === base) {
      return "/";
    }
    if (pathname.indexOf(base + "/") === 0) {
      return pathname.slice(base.length);
    }
    return pathname;
  }

  function prefixFor(pathname) {
    return pathname.indexOf(base + "/") === 0 || pathname === base ? base : "";
  }

  var rawPath = decodedPathname();
  var relativePath = stripBase(rawPath);
  var legacyMatch = relativePath.match(/^\/zip_contents\/([^/]+?)(?:\.md)?\/?$/);
  if (legacyMatch && legacyDetailAliases[legacyMatch[1]]) {
    window.location.replace(
      prefixFor(rawPath) +
        "/zip_contents/" +
        legacyDetailAliases[legacyMatch[1]] +
        "/" +
        window.location.search +
        window.location.hash
    );
    return;
  }

  var markdownMatch = relativePath.match(/^\/(.+)\.md$/);
  if (markdownMatch) {
    window.location.replace(
      prefixFor(rawPath) +
        "/" +
        markdownMatch[1] +
        "/" +
        window.location.search +
        window.location.hash
    );
  }
})();
</script>
